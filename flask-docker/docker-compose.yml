version: '3.8'

services:
    flaskapp:
        build: ./flaskapp-docker
        ports:
            -"8000:8000" #don't need this line because of the line below
        network_mode: host
        

        

